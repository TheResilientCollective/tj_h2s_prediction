# Pinned-ish deps for the report analysis. Install into a throwaway venv:
#   uv venv .venv && uv pip install -r requirements.txt
pandas>=2.2
numpy>=1.26
pyarrow>=15
matplotlib>=3.8
scipy>=1.11
statsmodels>=0.14
